// Trade-management tier (opt-in entry point: "openalgo-charts/trade").
// Phase 8+ implements order/position/bracket primitives, the DOM ladder,
// and the order state machine. Placeholder export keeps the bundle valid.

export const TRADE_TIER = 'trade' as const;
